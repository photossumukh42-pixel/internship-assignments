# Sumukh — Portfolio

Simple React + Vite portfolio scaffold.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```

This project contains a basic responsive `App` with sections: About, Projects, Skills, and Contact. Replace placeholder projects and text with your own details.
